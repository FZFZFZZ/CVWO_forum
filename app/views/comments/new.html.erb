<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
      body {
        background: ;
        font-family: 'Lucida Handwriting', cursive;
        background-color: rgba(244, 244, 244, 1);
        overscroll-behavior-y: none;
      }

      .container {
        margin-top: 300px !important;
        padding: 20px !important;
      }

      .tag {
        padding: 0em;
        margin: 0em;
        border-radius: 1em;
        display: inline-block;
      }
      
      .nav-link {
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
      }
      
      .hoverable {
        position: relative;
        display: inline-block;
      }
      
      .tipshown {
        visibility: hidden;
        opacity: 0;
        width: auto;
        max-width: 200px;
        white-space: nowrap;
        padding: 3px 6px;
        background-color: white;
        color: black;
        text-align: center;
        border-radius: 2px;
        position: absolute;
        top: 80%;
        z-index: 10;
        left: -60%;
        transform: translateX(-50%);
        transition: opacity 0.1s, visibility 0.1s;
        font-size: 13px;
      }
      
      
      .hoverable:hover .tipshown {
        visibility: visible;
        opacity: 1;
      }

    </style>
  </head>
  
  <body>
    <%= render partial: 'layouts/navall' %>
  
    <div class="container mt-4">
      <h2>My review</h2>
      
      <%= form_with model: [ @article, @comment ], local: true do |form| %>
        
        <div class="form-group">
          <%= form.text_area :body, required: true, class: 'form-control', 
                              placeholder: 'Leave your insightful / interesting / sarcastic review here...'%>
        </div>
    
        <div class="form-group form-check">
          <%= form.check_box :spoiler, class: 'form-check-input' %>
          <%= form.label :"Shh~ My review contains spoiler", class: 'form-check-label' %>
        </div>
    
        <%= form.submit 'Post', class: 'btn btn-primary' %>
    
      <% end %>
    </div>
  
  </body>

</html>