<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
      body {
        font-family: Arial;
        background-color: rgba(244, 244, 244, 1);
        overscroll-behavior-y: none;
        margin-top: 90px !important;
      }

      .container {
        padding: 40px !important;
        max-width: 70%;
      }

      .tag {
        padding: 0em;
        margin: 0em;
        border-radius: 1em;
        display: inline-block;
      }
      
      .search {
        max-width: 70% ;
        background-color: rgba(0,0,0,0.2);
        border-radius: 1em;
      }



      .nav-link {
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
      }
      
      .hoverable {
        position: relative;
        display: inline-block;
      }
      
      .tipshown {
        visibility: hidden;
        opacity: 0;
        width: auto;
        max-width: 200px;
        white-space: nowrap;
        padding: 3px 6px;
        background-color: white;
        color: black;
        text-align: center;
        border-radius: 2px;
        position: absolute;
        top: 80%;
        z-index: 10;
        left: -60%;
        transform: translateX(-50%);
        transition: opacity 0.1s, visibility 0.1s;
        font-size: 13px;
      }
      
      
      .hoverable:hover .tipshown {
        visibility: visible;
        opacity: 1;
      }


      .logo {
        max-width: 100%;
        max-height: 100%;
        display: flex;
        justify-content: center;
      }

      .count {
        display: flex;
        justify-content: center;
        font-size: 25px;
        text-shadow: 2px 2px 2px rgba(0, 0, 0, 0.3);
        margin-bottom: 30px !important;
      }

      .custom-sort-link {
        margin: 3px;
      }

    </style>
  </head>
  
  <body>
    <%= render partial: 'layouts/navall' %>

    <div class='container search'>

      <span class='logo'><%= image_tag 'Logo.png', alt: 'Logo', width: '250', height: '250' %></span>
      <span class='count'>Our forum has <%= Article.public_count %> movies and is updating...</span>
      
      <%= search_form_for @q do |f| %>
        <div class="form-group">
          <%= f.label :title_cont do %>
            <i class="fa fa-search" style="color: gold; font-size: 30px;"></i> <strong style="color: gold; font-size: 1.5em;">SEARCH</strong>
          <% end %>
          <%= f.search_field :title_cont, class: 'form-control', style: 'height: 50px;', 
                                          placeholder: 'Try "interstellar"' %>
        </div>
        
        <br/>
        <i class="fa fa-filter" style="color: royalblue; font-size: 30px;"></i> <strong style="color: royalblue; font-size: 1.5em;">FILTER</strong>
        <div class="form-group">
          <p style="display: inline; margin-right: 5px;">By Year</p>
          <div style="line-height: 2.5em;">
            <%= f.number_field :year_gteq, in: 1900..Date.today.year, class: 'form-control', style: 'width: 80px; display: inline-block;' %>
            <strong>~</strong>
            <%= f.number_field :year_lteq, in: 1900..Date.today.year, class: 'form-control', style: 'width: 80px; display: inline-block;' %>
          </div>
        </div>
        <%= f.label :tags_id_eq, 'By Tag' %>
      	<%= f.collection_select :tags_id_in, Tag.all, :id, :name, {}, { multiple: true, include_blank: true, class: 'form-select' } %>
        <br/>
        <%= f.submit "Show results", class: 'btn btn-success btn-lg' %>
        <%= link_to 'Reset', articles_path, class: 'btn btn-secondary btn-lg', method: :get %>
        <br/><br/>
        <i class="fa fa-sort-amount-down-alt" style="color: black; font-size: 30px;"></i> <strong style="color: black; font-size: 1.5em;">SORT</strong>

        <br/>
        <%= f.sort_link :title, 'Alphabetical', class: 'btn btn-outline-dark btn-sm custom-sort-link' %>
        <%= sort_link(@q, :average_rating, 'Average Rating', {}, { class: 'btn btn-outline-dark btn-sm custom-sort-link' }) %>
      
      <% end %>
    



    </div>

    <div class='container'>
    
    <ul class="list-group">
      <% @articles.each do |article| %>
        <li class="list-group-item">
          <%= link_to article.title, article_path(article), class: 'text-decoration-none', style: 'font-weight: bold;' %>
          <span style="color: rgba(0, 0, 0, 0.5);">(<%= article.year %>)</span>
        </li>
      <% end %>
    </ul>
    
    <br/>
    <%= paginate @articles %>

      <% if admin_user? %>
        <%= link_to 'Add a new movie', new_article_path, class: "btn btn-success" %>
      <% end %>

    </div>
  </body>
</html>