<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
      body {
        font-family: 'Lucida Handwriting', cursive;
        background-color: rgba(244, 244, 244, 1);
        overscroll-behavior-y: none;
        margin-top: 90px !important;
        text-align: center;
      }

      .container {
        padding: 40px !important;
        max-width: 60%;
        justify-content: center;
        align-items: center;

      }

      .tag {
        padding: 0em;
        margin: 0em;
        border-radius: 1em;
        display: inline-block;
      }
      
      .search {
        max-width: 70% ;
        background-color: rgba(0,0,0,0.2);
        border-radius: 1em;
      }


      .nav-link {
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
      }
      
      .hoverable {
        position: relative;
        display: inline-block;
      }
      
      .tipshown {
        visibility: hidden;
        opacity: 0;
        width: auto;
        max-width: 200px;
        white-space: nowrap;
        padding: 3px 6px;
        background-color: white;
        color: black;
        text-align: center;
        border-radius: 2px;
        position: absolute;
        top: 80%;
        z-index: 10;
        left: -60%;
        transform: translateX(-50%);
        transition: opacity 0.1s, visibility 0.1s;
        font-size: 13px;
      }
      
      
      .hoverable:hover .tipshown {
        visibility: visible;
        opacity: 1;
      }

      #articlesCarousel {
        width: 60%;
        height: 700px;
        margin-top: 40px;
        margin-left: auto;
        margin-right: auto;
        display: block;
        border-radius: 5px !important;
        margin-bottom: 40px ;
      }
      
      .carousel-item {
        height: 700px;
      }
      
      .carousel-item img {
        height: 100%;
      }

      .carousel-caption {
        background-color: rgba(0, 0, 0, 0.7);
        color: white;
        padding: 10px;
        bottom: 0%;
        left: 50%;
        transform: translateX(-50%);
        width: 102%;
        text-align: center;
      }
    </style>

  </head>
  
  <body>
  <%= render partial: 'layouts/navall' %>


  <p style="color: green"><%= notice %></p>
  
    <div class='container'>
      <ul class="list-group vertical-list" >
        <li class="list-group-item" style="background-color: rgba(0,0,0,0.7); color: white;">
          <%= render @tag %>
        </li>
        <% if admin_user? %>
          <li class="list-group-item">
            <%= link_to "Edit this tag", edit_tag_path(@tag), class: 'btn btn-primary btn-sm' %>
            <%= link_to "Back to tags", tags_path, class: 'btn btn-secondary btn-sm' %>
          </li>
          <li class="list-group-item">
            <%= button_to "Destroy this tag", @tag, method: :delete, form: { class: 'd-inline' }, class: 'btn btn-danger btn-sm' %>
          </li>
        <% end %>
        <li class="list-group-item">
          <%= link_to 'Search for More', articles_path, class: 'btn btn-success', method: :get %>
        </li>
      </ul>
    </div>



  </body>
</html>